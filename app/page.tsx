// Root page - handled by middleware which redirects to /[locale]/
// This file is intentionally minimal as the middleware intercepts all requests
export default function RootPage() {
  return null
}
